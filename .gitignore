# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Test runner artifacts
test-results
playwright-report
coverage

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code – nur maschinenspezifische Artefakte ignorieren.
# .claude/commands/ wird bewusst getrackt (Issue #31) → NICHT ignorieren.
.claude/settings.local.json
.claude/cache/
.claude/memory/

# Secrets / credentials (Pflichteinträge gitignore-audit)
.env
.env.local
credentials.json
*.jks
*.keystore
*.p12
*.p8

# Interne Doku darf nicht getrackt werden (Pflichteintrag gitignore-audit)
docs/private/
